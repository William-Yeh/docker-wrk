machine:
  services:
    - docker

dependencies:
  pre:
    - docker info
    - docker build  -t build-rootfs  -f Dockerfile.rootfs  .
    - docker run  -v $(pwd):/data  build-rootfs 
  override:
	- docker build  -t williamyeh/wrk  .

test:
  override:
    - docker run  williamyeh/wrk  https://www.google.com/
    - docker run  williamyeh/wrk  https://www.facebook.com/
